# 七配置补测与进程退出后启动（2026-09-15）

本轮新增六配置 × 两种段落 × 四种输入尺寸 × 五次调用，共 **240 次真实 NSPerformService 调用**，全部成功且完整字节数／SHA-256 匹配锁定的同版本 CLI 期望。与上级目录此前 s2t 的 40 次相加，七配置的 56 组语料已完成 280 次调用。本轮没有修改应用代码、依赖或容量规则。

## 测量条件

- 沿用 `eab5003bfdd938f08b936826225d7ab482d6f703` 的 Release universal 包，实际签后二进制 SHA-256 `11b54d162172123fbf742441f7188d74176df9360b8e53ad7bcfffd61b526e21`。来源、工具链和本机硬件见上级目录；本次调用 metadata 再次记录 macOS 27.0（26A428）。这是独立 ad-hoc 验证安装，不是最终分发包。
- 新启动提供者 PID 80200；六组均在同一进程中，依次为 s2tw、s2twp、s2hk、legacy-s2hk-tw-idiom、t2s、legacy-s2t-tw-idiom。每组通过真实配置 UI 切换，检查选中状态和持久偏好，关闭设置面板，再开始 CLI 测量。注册信息见 [registration-excerpt.txt](registration-excerpt.txt)，仅保留此测试服务的字段。
- 每个配置先做一次 1 KiB priming，再按短段落、长段落和递增尺寸执行。priming 单独保存，不混入统计。每个数据集启动一个 CLI 调用方，连续五次调用，不人为等待；期间不做 AX 查询或录屏。没有采集全程热状态或外部负载，不能视为受控跨配置性能竞赛。
- 简体测量通过目标语言 Segment 选择：先前保存的香港字形和台湾词汇仍保留但 UI 禁用，最终输出与 t2s 完整相符。此结果不把简体称为完整的台湾词汇反向转换。
- 调用耗时只覆盖同步 API 返回，不含调用后读取、编码、hash 或字节比较，也不是界面完成排版／恢复编辑的耗时。前一组 s2t 的进程不同，保留各自上下文，不合并计算跨配置总中位数或升级收益。

## 10 MiB 结果摘要

所有数值均为五次调用的中位数，单位 ms；1 KiB、1 MiB、5 MiB 及全部最小／最大值见 [summary.json](summary.json)。不丢弃任何慢样本。

| 配置 | 短段落 | 长段落 |
| --- | ---: | ---: |
| s2tw（台湾字形） | 524.575 | 524.902 |
| s2twp（台湾字形＋词汇） | 627.734 | 628.399 |
| s2hk（香港字形） | 522.638 | 529.779 |
| legacy-s2hk-tw-idiom（香港字形＋台湾词汇） | 836.205 | 836.174 |
| t2s（简体） | 421.454 | 418.983 |
| legacy-s2t-tw-idiom（OpenCC 字形＋台湾词汇） | 736.361 | 732.811 |

原始记录、每组上下文和 priming 在对应配置目录。归档时重新核对了 48 组 metadata 与语料 manifest、全部 240 条成功记录及输出 hash，并从原始时间重算中位数／最小值／最大值，与运行时摘要逐组一致；见 [verification.json](verification.json)。[执行脚本快照](run-configuration-series.py)保留实际运行流程，其中 PID、路径及既有配置检查是本轮的条件，不能不经修改和确认就用于新安装；脚本不切换 UI 或自动重启失败的提供者。

## 应用退出后的系统启动

通过 CUA 正常退出 PID 80200，`ps` 确认进程消失，服务仍注册；此后没有手动启动或 AX 查询，直接调用真实服务。单次 s2t／1 KiB 请求成功，完整输出正确，API 往返 **350.647458 ms**；系统启动了同一安装路径的新进程 PID 84380。

[调用前](startup-before.json) · [原始调用](startup-s2t-1024.jsonl) · [调用后进程](startup-after.json)。这是进程退出后的单次启动样本，未清空系统文件缓存，不是冷机冷启动统计，不能外推 10 MiB 启动场景。

之后 AX 仍观察到默认文稿与空结果，继续关联 #19。一次 Cmd-W 后的 AX 查询重新观察到了窗口，没有取得稳定的“进程运行、所有窗口关闭”前置条件，因此不把该尝试标为已关窗验收。另一次本地截图的布局与 AX／窗口尺寸枚举不一致，未把它作为可发布的尺寸或布局验收证据。

## CI 与保留项

源码／证据提交 `6153c7b` 的 [App Regression](https://github.com/gewill/OpenCCman/actions/runs/34952079607) 已成功；[日志节选](ci-evidence.txt)确认九项脚本失败传播测试、58 个 Swift 文件解析、真实控件检查、原生滚动断言以及 Mac 完整构建实际执行。两个手动构建 job 为 skipped，不声称本次另做了 iOS 或 Release 构建。此前滚动失败仍保留 #20，不能用本次通过宣称根因已修复。

#22 仍缺：TextEdit 和第二个实际目标 App 的选区替换／恢复编辑／失败原稿保留、稳定的已关窗调用及更多启动场景、超时与失败行为、容量产品决定。#81 保持草稿。

[清理结果](cleanup.json)：两个测试进程均已退出，专用安装移回隔离工作区，服务注册已移除，测试偏好恢复为不存在，VoiceOver 保持关闭。未终止或激活 #19 的原有基线进程。
