{
  "status": "Not a valid clean paired comparison",
  "actual": "Setting private lastPresentedWhatsNewVersion before launch did not suppress the delayed sheet; first explicit AX read again found the sheet",
  "action": "Dismissed Done through UI. Retain this attempted run; exclude its pre-AX memory sample from performance comparison. Do not infer the reason for preference behavior.",
  "subsequent_diagnosis": "NativeWindowLifecycleAudit.prepare() explicitly removes its private persistent domain at every launch, overriding the external preference setup. Subsequent protocol dismisses the actual sheet before import."
}
